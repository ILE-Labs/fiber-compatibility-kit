# Evidence fixtures

Fixtures in this directory are schema examples only. They are not evidence of a real Fiber-version difference. Real snapshots must be captured from two running Fiber versions and must include the version, commit/tag, scenario, RPC method outputs, and capture command.
